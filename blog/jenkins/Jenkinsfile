pipeline {
    agent any
    environment {
        RELEASE = "TEST"
    }
    stages {
        stage('Build') {
            environment {
                LOG_LEVEL = "INFO"
            }
            steps {
                
                        sh """
                        . .env/bin/activate
                        pip install -r requirements.txt
                        """
            
            }
        }
    }
}
