pipeline {
    agent any
    environment {
        RELEASE = "TEST"
    }
    stages {
        stage('Checkout') {
            steps {
            checkout scm 
            }
        }
        stage('Setup') {
            script {
          sh """
          pip install -r requirements.txt
          """
        }
        }
    }
}
